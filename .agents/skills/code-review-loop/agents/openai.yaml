interface:
  display_name: "Code Review Loop"
  short_description: "Iterative review-to-issues cleanup loop"
  default_prompt: "Use $code-review-loop to repeatedly review this codebase, convert findings into issues, and close the slices until only marginal findings remain."
